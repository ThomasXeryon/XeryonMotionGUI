<Page
    x:Class="XeryonMotionGUI.Views.MotionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XeryonMotionGUI.Views"
    xmlns:viewmodels="using:XeryonMotionGUI.ViewModels">

    <Page.DataContext>
        <viewmodels:MotionViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <!-- Narrow column for the ListView -->
            <ColumnDefinition Width="*" />
            <!-- Wider column for details -->
        </Grid.ColumnDefinitions>

        <!-- ListView for controllers on the left -->
        <ListView 
            ItemsSource="{Binding RunningControllers}" 
            SelectedItem="{Binding SelectedController, Mode=TwoWay}"
            Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10">
                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Type}" FontSize="14" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Right side: Axis Details for selected controller -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Axis Details" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Displaying details only if a controller is selected -->
            <StackPanel Visibility="{Binding SelectedController, Converter={StaticResource NullToVisibilityConverter}}" Margin="0,20,0,10">
                <ItemsControl ItemsSource="{Binding SelectedController.Axes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Axis Name:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Name}" />
                                <TextBlock Text="Resolution:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Resolution}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
